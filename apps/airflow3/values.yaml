# apps/airflow3/values.yaml

createUserJob:
  useHelmHooks: false

migrateDatabaseJob:
  useHelmHooks: false

config:
  core:
    load_examples: "true"
  api:
    expose_config: "true"

env:
  - name: AIRFLOW__CORE__LOAD_EXAMPLES
    value: "True"