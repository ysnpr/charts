# apps/airflow3/values.yaml

createUserJob:
  useHelmHooks: true

migrateDatabaseJob:
  useHelmHooks: true

config:
  core:
    load_examples: "true"
  api:
    expose_config: "true"

env:
  - name: AIRFLOW__CORE__LOAD_EXAMPLES
    value: "True"